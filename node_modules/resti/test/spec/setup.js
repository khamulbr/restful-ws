describe('resti', function() {

	beforeEach(function() {
        swapi = new resti('http://swapi.co/api/');
	});

	it('should throw an error if no URL is passed', function() {
		try {
			var api = new resti();
		} catch (err) {
			expect(err.message).toEqual('resti must be passed a base URL')
		}
	});

});